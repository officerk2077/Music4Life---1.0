<UserControl x:Class="music4life.Views.ArtistsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="ArtistCardStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,0,0,20">

            <TextBlock Text="Artists" FontSize="32" FontWeight="Bold" Foreground="White" Margin="15,10,0,20"/>

            <ItemsControl ItemsSource="{Binding ArtistList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ArtistCardStyle}" 
                                Click="ArtistCard_Click"
                                Margin="15,0,20,30" 
                                Width="160">

                            <StackPanel>
                                <Border Width="160" Height="160" Background="#333" CornerRadius="80">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="10" Opacity="0.3" ShadowDepth="4"/>
                                    </Border.Effect>

                                    <Grid>
                                        <Image Source="{Binding ArtistImage}" Stretch="UniformToFill">
                                            <Image.Clip>
                                                <EllipseGeometry Center="80,80" RadiusX="80" RadiusY="80"/>
                                            </Image.Clip>
                                        </Image>

                                        <Path Data="M12,4A4,4 0 0,1 16,8A4,4 0 0,1 12,12A4,4 0 0,1 8,8A4,4 0 0,1 12,4M12,14C16.42,14 20,15.79 20,18V20H4V18C4,15.79 7.58,14 12,14Z" 
                                              Fill="#555" Stretch="Uniform" Width="60" Height="60"
                                              HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Path.Style>
                                                <Style TargetType="Path">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ArtistImage}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Path.Style>
                                        </Path>
                                    </Grid>
                                </Border>

                                <TextBlock Text="{Binding Name}" 
                                           Foreground="White" FontWeight="Bold" FontSize="15"
                                           Margin="0,10,0,4" TextTrimming="CharacterEllipsis" 
                                           TextAlignment="Center" HorizontalAlignment="Stretch"
                                           ToolTip="{Binding Name}"/>

                                <TextBlock Text="{Binding SongCountText}" 
                                           Foreground="#B3B3B3" FontSize="13" 
                                           TextAlignment="Center" HorizontalAlignment="Stretch"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>